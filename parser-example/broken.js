// Incomplete import
import { something, anotherThing from "./utils

// Missing closing brace, missing parentheses
function loadUser(id, options {
  console.log("Loading user");
  if (id > 0
    console.log("Valid ID")
  else
    console.log("Invalid ID");
  
  // Unfinished try-catch block
  try {
    const data = fetchData(id)
    console.log("Fetched", data)
  catch (e) 
    console.error("Error", e)

// Broken arrow function and missing body
const getUser = (id => 
  id * 2
)

// Unclosed string and invalid template literal
const msg = "User data is: 
const template = `User ${id} data: ${`

// Broken object literal
const user = {
  id,
  name: getName(id),
  preferences: {
    theme: "dark",
    layout: 
  }
}

// Missing function keyword entirely
updateUser(id) {
  return id + 10
}

// Invalid class and missing methods
class User {
  constructor(id {
    this.id = id
  }

  getInfo( {
    return "ID: " + this.id
  }
}

// Bad call expression
doSomething(

// Random tokens that break everything
@@ @@@ ??? !! ???
